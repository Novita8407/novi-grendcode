for (let index = 1; index <= 100; index += 2) {
  console.log(index);
}

let fruits = ["mangga", "rambutan", "jambu"];
for (let index = 1; index < 3; index++) console.log(fruits[index]);

for (let lastindex = 0; index < fruits.length; index++) {
  console.log(fruits[index]);
}
